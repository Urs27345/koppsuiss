name: Build Blutdruck APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'public/blut/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Replace with final workflow for this repository."
